String are immutable

Example:

let str = "Kishor"

str[2] = "J"; // This won't work.

String Methods and properties

str.length;
str.slice(start_index, end_index);
str.substring(start, end) // Do not include -ve indexes.
str.toUpperCase();
str1.concat(str2);
str.trim();

srt.indexof("i");
str.chatAt(index);
str.charCodeAt(index); // Ascii code of character.
